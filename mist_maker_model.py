import relay

class MistMakerModel():
	def __init__(self,interval,nb_repeat):
		self.x, self.y = []
		self.interval = interval
		self.sleeping = 60
	
	def measure(self):
		for in 
	def estimate(self):
		